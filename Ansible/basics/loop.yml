---
- name: Install multiple packages
  hosts: webservers
  gather_facts: yes

  tasks:
    - name: Install packages
      apt:
        name: "{{ item }}"
        state: present
      become: yes
      loop:
        - git
        - curl
        - vim
